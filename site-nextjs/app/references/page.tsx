import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { caseStudies } from "@/lib/content";
import { CTABanner, HeroSection } from "@/components/UI";
export const metadata:Metadata={title:"Références d’installation de bornes électriques",description:"Exemples d’installations de bornes électriques pour flottes, commerces et sites industriels en Belgique et dans les Hauts-de-France."};
export default function References(){return <><HeroSection eyebrow="Références" title="Des déploiements mesurés par leurs résultats." description="Volumes, délais, disponibilité et usage : découvrez comment nous transformons une ambition d’électrification en programme opérationnel."/><section className="py-24"><div className="container grid gap-6 md:grid-cols-2 lg:grid-cols-3">{caseStudies.map(c=><article key={c.slug} className="card flex flex-col"><p className="text-sm font-bold text-electric-600">{c.sector}</p><h2 className="mt-3 text-2xl font-black text-navy-950">{c.client}</h2><p className="mt-4 leading-7 text-slate-600">{c.summary}</p><div className="mt-7 grid grid-cols-2 gap-3 text-sm"><p><b className="block text-navy-950">{c.count}</b>installées</p><p><b className="block text-navy-950">{c.duration}</b>de déploiement</p></div><Link href={`/references/${c.slug}`} className="mt-8 inline-flex items-center gap-2 font-bold text-navy-950">Voir le cas <ArrowRight size={17}/></Link></article>)}</div></section><CTABanner/></>}
